/**
 * resources/js/app.js
 *
 * Frontend JS entry point, bundled by Vite. Currently empty — this app is
 * server-rendered Blade + Bootstrap, so there's no client-side framework
 * to bootstrap. This file exists so the frontend/backend split has a real
 * home for JS if you add interactivity later.
 */

console.log('CRDB Segmentation frontend assets loaded.');
